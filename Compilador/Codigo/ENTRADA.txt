begin
##
  int num;
  int fat;
##  
  scan(num);
  if(num < 0) do
    fat = 0;
    print(id);
  endif

  while(num > 2) do
    fat = 2;
  endwhile
##
    fat = 0;
  else do
    fat = 1;
    while(num > 1) do
      fat = fat * num;
      num = num - 1;
    endwhile

    print(fat);
  endif
##
end